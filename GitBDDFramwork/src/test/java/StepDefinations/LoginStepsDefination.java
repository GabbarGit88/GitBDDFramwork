package StepDefinations;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.Assert;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class LoginStepsDefination {

	/*
	 * WebDriver driver;
	 * 
	 * @Given("User is on Login Page") public void user_is_on_login_page() {
	 * System.setProperty("webdriver.chrome.driver",
	 * "C:\\Users\\Orangebits\\Downloads\\Compressed\\chromedriver.exe"); driver =
	 * new ChromeDriver();
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(30));
	 * driver.manage().window().maximize();
	 * driver.get("https://www.saucedemo.com/v1/");
	 * 
	 * }
	 * 
	 * //@When("User enter valid username and password") //public void
	 * user_enter_valid_username_and_password() {
	 * 
	 * @When("User enter valid {string} and {string}") public void
	 * user_enter_valid_and(String username, String password) throws
	 * InterruptedException {
	 * 
	 * driver.findElement(By.className("form_input")).sendKeys(username);
	 * driver.findElement(By.id("password")).sendKeys(password); Thread.sleep(3000);
	 * }
	 * 
	 * @And("Clicks on Login Button") public void clicks_on_login_button() {
	 * driver.findElement(By.id("login-button")).click(); }
	 * 
	 * @Then("User navigated to Home Page") public void
	 * user_navigated_to_home_page() {
	 * 
	 * WebElement e = driver.findElement(By.className("app_logo")); if
	 * (e.isDisplayed()) { System.out.println("app_logo is Visible"); } else {
	 * System.out.println("app_logo is not Visible"); } }
	 * 
	 * @And("Close the Browser") public void close_the_browser() { driver.quit(); }
	 */

}
